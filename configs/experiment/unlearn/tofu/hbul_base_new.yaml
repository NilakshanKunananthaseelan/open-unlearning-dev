# @package _global_

defaults:
  - override /model: Llama-3.2-1B-Instruct
  - override /trainer: HBULBaseNew
  - override /data: unlearn
  - override /data/datasets@data.forget: TOFU_QA_forget
  - override /eval: tofu

model:
  model_args:
    pretrained_model_name_or_path: /home/nilakshan/0-Unlearning/0-open-unlearning-dev/saves/finetune/tofu_Llama-3.2-1B-Instruct_full
forget_split: forget10
retain_split: retain90
holdout_split: holdout10
retain_logs_path: null
question_key: "question"

eval:
  tofu:
    forget_split: ${forget_split}
    holdout_split: ${holdout_split}
    retain_logs_path: ${retain_logs_path}
    overwrite: true
    question_key: ${question_key}
    
data:
  anchor: forget
  forget:
    TOFU_QA_forget: 
      args:
        hf_args:
          name: ${forget_split}

trainer:
  args:
    warmup_epochs: 1.0
    learning_rate: 2e-5  # Increased learning rate for stronger unlearning
    weight_decay: 0.01
    num_train_epochs: 30  # Reduced epochs but with stronger unlearning
    

    # warmup_epochs: 1.0
    # learning_rate: 5e-6  # Increased learning rate for stronger unlearning
    # weight_decay: 0.01
    # num_train_epochs: 30  


task_name: ???